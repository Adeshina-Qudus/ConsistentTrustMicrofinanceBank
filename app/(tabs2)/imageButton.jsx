import {Image} from "react-native";
import image from "../../constants/image"
const CustomImageButton = ()=>{
    return(
        <Image source={image.MiddleButton} />
    )
}
export default CustomImageButton